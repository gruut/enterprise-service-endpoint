syntax = "proto3";

package grpc_se;

service  GruutSeService {
   rpc transaction(GrpcMsgTX) returns (TxStatus) {}
}
message GrpcMsgTX {
   bytes message = 1;
}
message TxStatus {
   bool message = 1;
}
